#twig_express:
#    twig_express:


# (Optional) Add to Twig global variables, using a namespace to avoid collisions
# (especially if you have more than one bundle with 'static' views).
twig:
    globals:
        twig_express:
            test: 'It works.'

# (Optional) Enable bundle for Assetic and TwigExpressBundle use
assetic:
    bundles: [ TwigExpressBundle ]
