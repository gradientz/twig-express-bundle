{% extends twigexpress_layout() %}
{% set metaTitle = 'The markdown() function' %}

{% block content %}
  <h1>The <code>markdown()</code> function</h1>

  <h2><code>markdown('Hello *world*!')</code></h2>
  <blockquote class="TestResult">
  {{ markdown('Hello *world*!') }}
  </blockquote>

  <h2><code>markdown('Hello *world*!', inline=true)</code></h2>
  <blockquote class="TestResult">
  {{ markdown('Hello *world*!', inline=true) }}
  </blockquote>

  <h2><code>markdown('Hello *world*!') | raw</code></h2>
  <blockquote class="TestResult">
  {{ markdown('Hello *world*!') | raw }}
  </blockquote>

  <h2><code>markdown(source('@KaliopTwigExpressBundle/README.md')) | raw</code></h2>
  <blockquote class="TestResult">
  {{ markdown(source('@KaliopTwigExpressBundle/README.md')) | raw }}
  </blockquote>
{% endblock %}
